apiVersion: v1
kind: Service
metadata:
  name: vostra-ai-extractor
  namespace: vostra-invoice-web
  labels:
    app: vostra-ai-extractor
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: vostra-ai-extractor
