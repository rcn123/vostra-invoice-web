apiVersion: v1
kind: Service
metadata:
  name: vostra-api
  namespace: vostra-invoice-web
  labels:
    app: vostra-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vostra-api
