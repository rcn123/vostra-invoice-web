apiVersion: v1
kind: Service
metadata:
  name: vostra-landing
  namespace: vostra-invoice-web
spec:
  selector:
    app: vostra-landing
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
