2. Format of the Sannolikhet column (visible state):

Display a percentage value, e.g. 82 %

Display an information icon (i) next to the percentage

All user-facing text must be in Swedish

Example cell:

82 %  (i)


3. Behavior when clicking (i) in the Sannolikhet column:
Clicking the icon should open a small collapsible explanation box directly below that invoice row (do not use a modal or overlay).

This box must show the AI explanation (XAI) for the currently selected account in Swedish.

Suggested layout inside the explanation box:

Förklaring:
• Matchade ord: ”kontorsmaterial”, ”pennor”
• Liknande historik: 37 fakturor
• Osäkerhet: låg
• Underlag: radtext + tidigare feedback


This explanation must update dynamically if the user changes the selected account in the dropdown.

4. Add probabilities to each alternative in the dropdown list:
When the user opens the dropdown to choose another konto, each alternative must show:

Konto number + label

Probability percentage

A subtle, thin background bar representing the probability (very light pastel color)

Example dropdown entry:

[thin pastel-green bar width 82%]
5110 – Kontorsmaterial        82 %


Do NOT show explanations inside the dropdown — only in the collapsible explanation box triggered by (i).

5. When user selects a new konto from the dropdown:

Update the displayed selected account

Update the Sannolikhet value accordingly

Update the explanation box (if open) to show explanation for the newly selected account

6. Visual style requirements:

Clean, modern, Swedish-government/municipal aesthetic

No heavy borders, no 2000s-style progressbars

All Swedish text, concise and neutral

Explanation box must be minimal and unobtrusive

7. Purpose of this change:
We want:

top-1 account + its probability (visible)

top-N alternatives with probabilities (dropdown)

explainability (clickable)

minimal cognitive load