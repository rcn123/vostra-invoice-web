version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: vostra-frontend

  # Backend placeholder - uncomment when FastAPI is ready
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8000:8000"
  #   restart: unless-stopped
  #   container_name: vostra-backend
  #   environment:
  #     - DATABASE_URL=${DATABASE_URL}
